# prodOrag — flat, install-ready requirements file.
#
# `pip install -r requirements.txt` installs every third-party dependency
# needed to run the API, ingest a corpus, and use the Streamlit dashboard.
# Requires Python >= 3.12.
#
# This only installs dependencies, not the `rag_pipeline` package itself —
# follow it with `pip install -e . --no-deps` (or just use
# `pip install -e .` on its own, which reads pyproject.toml and covers the
# same ground).
#
# This mirrors pyproject.toml (the source of truth for `pip install -e .`).
# If you change one, change the other.

# web service
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
slowapi>=0.1.9

# config & validation
pydantic>=2.9
pydantic-settings>=2.5
python-dotenv>=1.0
PyYAML>=6.0

# LangChain stack
langchain>=0.3
langchain-core>=0.3
langchain-ollama>=0.2
langchain-community>=0.3
langchain-chroma>=0.1.4
langchain-text-splitters>=0.3

# vector store + embeddings + reranker
chromadb>=0.5
sentence-transformers>=5.4
transformers>=4.45
rank-bm25>=0.2.2
FlagEmbedding>=1.3

# parsing
docling>=2,<2.92        # DocumentConverter API removed in 2.92 (see src/rag_pipeline/parsers/docling.py)
pdfplumber>=0.11         # concordance table parsing + PDF page-image preview

# data
pandas>=2.2
openpyxl>=3.1

# ── optional: notebooks (notebooks/*.ipynb) ──────────────────────────
jupyter==1.1.1
ipykernel==6.29.5

# ── optional: eval / RAGAS A/B notebooks (`rag_pipeline.eval`) ───────
ragas>=0.2
datasets>=3.0
matplotlib
# ragas 0.4.x unconditionally imports langchain_community.chat_models.vertexai,
# which langchain-community dropped in 0.4.2 — cap until ragas fixes this upstream.
langchain-community<0.4.2

# ── optional: cloud model providers (uncomment as needed) ────────────
# langchain-openai>=0.2
# langchain-aws>=0.2
# langchain-google-vertexai>=2.0

# ── dev / test ────────────────────────────────────────────────────────
pytest>=8
pytest-cov>=5
httpx>=0.27
